{:itonami.blueprint/id "cloud-itonami-iso3166-dnk"
 :itonami.blueprint/name "Independent Public-Sector Market-Entry & Procurement Compliance Service — Denmark"
 :itonami.blueprint/iso3166 "DNK"
 :itonami.blueprint/domain :public-sector/market-entry-compliance
 :itonami.blueprint/license "AGPL-3.0-or-later"
 :itonami.blueprint/social-impact [:eu-single-market-access :public-spend-transparency :cross-border-friction-reduction]
 :itonami.blueprint/governor :market-entry-compliance-governor
 :itonami.blueprint/robotics false
 :itonami.blueprint/required-technologies [:identity :forms :dmn :bpmn :audit-ledger]
 :itonami.blueprint/status :public-oss
 :itonami.blueprint/maturity :implemented
 :itonami.blueprint/implemented-slice
 "Full Actors-pattern implementation: MarketEntry-LLM advisor (marketentry.marketentryllm) sealed behind an independent Market-Entry Compliance Governor (marketentry.governor, 7-check: spec-basis, evidence-incomplete, eu-establishment-missing [flagship, independently verifies EU/Danish establishment via :has-eu-establishment? for engagements that declare :requires-eu-establishment? true], engagement-fee-mismatch, cvr-unverified, already-drafted, already-submitted) driving a langgraph-clj StateGraph (marketentry.operation) over an append-only audit ledger; MemStore + langchain-store-backed DatomicStore (marketentry.store, built on io.github.kotoba-lang/langchain-store per ADR-2607141600, not a hand-rolled enc/dec* codec); marketentry.facts grounded in a verified Denmark dossier (Udbud.dk public-procurement portal, Udbudsloven, CVR company registration); governor contract + store parity + registry + phase + facts tests all passing (clojure -M:dev:test); 0 clj-kondo errors (clojure -M:lint); CI (.github/workflows/ci.yml) reconstructs the kotoba-lang/langgraph+langchain+langchain-store sibling-checkout layout."
 ;; Deployed into the ai-gftd-repository-dispatch namespace (Workers for
 ;; Platforms). A user Worker there has no URL of its own — it is reachable
 ;; only through the dispatch Worker, so the address is the router plus this
 ;; actor's repository name, which is also the catalog's :repo key.
 ;; /health answers JSON, so it is declared as the probe path.
 :itonami.blueprint/endpoint "https://itonami-fleet-dispatch.04-feasts-minded.workers.dev/cloud-itonami-iso3166-dnk"
 :itonami.blueprint/endpoint-kind :dispatch-namespace
 :itonami.blueprint/health-path "/health"}
